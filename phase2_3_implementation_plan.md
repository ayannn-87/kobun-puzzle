# 雅ラン 実装案（世界観再設計 & 助動詞識別ゲーム）

ご確認いただいた方針に沿って、実装を3フェーズに分けてご提案します。**フェーズ1（キャラクター成長・身分統合）は今回すでに実装済み**です。フェーズ2・3の具体的な実装案を以下にまとめます。

---

## フェーズ1（実装済み）：身分システム統合 ＋ キャラクター立ち絵

- 身分システムを5段階に統合しました：見習い(Lv1-5)→学習者(Lv6-11)→上級学習者(Lv12-18)→学者(Lv19-25)→国学博士(Lv26〜)
- 添付いただいたキャラクターシートを10枚（姫Lv1〜5／貴公子Lv1〜5）に個別切り出し・透過処理し、レベルに応じて自動的に該当段階の立ち絵が表示されるようにしました（常時HUD・レベルアップ演出・キャラクター選択画面すべて対応）
- 「伝説級」の特別演出は「国学博士」到達時の演出として据え置き（金の脈動する光＋専用セリフ）

---

## フェーズ2：世界観再設計（書院モチーフへの移行）

### 2-1. 背景の再構築

現在の`#night-sky`（星＋月）を`#study-room`に置き換える方針です。

| 現行 | 変更後 |
|---|---|
| 夜空グラデーション（紺〜紫） | 障子越しの陽光を思わせる、生成り〜淡い金のグラデーション |
| 星（radial-gradientの光点） | 廃止 |
| 月（発光する円） | 廃止し、右上に巻物または文箱のシルエットを配置 |
| 桜の花びらエフェクト | 廃止し、代替演出に置き換え（下記2-3） |

CSS変数の再定義案：

```css
:root{
  /* 旧：夜空系カラー → 新：書院・陽光系カラー */
  --room-bg-top: #f3ead2;      /* 障子越しの明るい生成り */
  --room-bg-mid: #e8dcb8;
  --room-bg-deep: #cdb98a;     /* 畳・木部を思わせる濃さ */

  /* アクセントカラーを藍・朱中心 → 深緑・えんじ・墨・金中心へ */
  --wood: #6b4a2f;             /* 床柱・木部 */
  --tatami: #cdbd8a;           /* 畳の縁 */
  --forest: #3e5c46;           /* 深緑（男子キャラの装束とも統一感） */
  --enji: #7a2e34;             /* えんじ色 */
  --sumi: #2a2116;             /* 墨（既存の --ink をそのまま流用可） */
  --gold: #c9a24b;             /* 金（既存カラーを継続使用） */
}
```

### 2-2. 「和紙パネル」はそのまま活用

既存の`.washi-panel`（生成り色＋斜め罫の透かし模様）は今回の方向性と矛盾しないため、**大きな変更は不要**です。変更するのは枠線・アクセント色のみ：

```css
.washi-panel{
  /* 既存の斜め罫テクスチャ・グラデーションは維持 */
  border: 1px solid var(--wood);          /* 藍系の線色 → 木部色に */
  box-shadow: 0 0 0 1px rgba(201,162,75,0.4), 0 24px 50px -28px rgba(0,0,0,0.5);
}
```

### 2-3. 装飾モチーフの置き換え

| 現行要素 | 置き換え案 | 実装方法の目安 |
|---|---|---|
| `#stars`（星） | 廃止 | 要素ごと削除 |
| `#moon`（月） | 巻物／文箱のシルエット | 既存の発光circle実装を、巻物形状のSVG or 角丸長方形に差し替え。box-shadowによる柔らかい光はそのまま「陽光」として転用可 |
| `.petal`（桜の花びら） | 墨の筆致の粒子、または庭園越しの小さな桜（画面端に控えめに） | 既存のfall/swayキーフレームはそのまま使い、形状（border-radius）と色（墨色 or 控えめな桜色）のみ変更。数を現行22個→10個程度に減らし主張を抑える |
| タイトルパネルの上部装飾ライン | 巻物の軸を思わせる二重線に強化 | 既存の`.title-panel::before`を拡張 |

### 2-4. 成長段階と連動した背景演出（ご提案の「格上げ」の具体化）

CSSクラスの重ね掛けだけで実現可能な設計です。`renderCharacterHud()`内で`getGrowthStageForLevel(playerData.level)`の値を`document.body`に`data-stage="1"`〜`"5"`として付与し、CSSの属性セレクタで背景演出を切り替えます。

```js
// renderCharacterHud() 内に追加する想定
document.body.dataset.stage = String(getGrowthStageForLevel(playerData.level));
```

```css
/* 段階が上がるごとに、パネルの縁取りや背景装飾を格上げする */
body[data-stage="1"] .washi-panel{ /* 素朴な木目のみ */ }
body[data-stage="2"] .washi-panel::after{ /* 本棚のシルエットを薄く追加 */ }
body[data-stage="3"] .washi-panel{ border-width: 2px; } /* 二重線の縁取りに */
body[data-stage="4"] .washi-panel::after{ /* 巻物の束を追加 */ }
body[data-stage="5"] .washi-panel{ box-shadow: 0 0 0 1px var(--gold), 0 0 24px rgba(201,162,75,0.3); } /* 金の輝きを追加 */
```

新規イラストを都度用意する必要がなく、既存のCSS構造の拡張だけで実現できます。

### 2-5. 演出の置き換え（パーティクル→学問演出）

| 現行の演出 | 置き換え案 |
|---|---|
| `is-glow`（行灯の光） | 印判（判子）を押す演出：拡大→着地のバウンドで、朱色の丸が「ポン」と押される表現に変更 |
| レベルアップモーダルの表示 | 巻物が横に開くアニメーション（`transform: scaleX()`を0→1にアニメーション） |
| 完成テキストの強調 | 筆致のアンダーライン（SVGの`stroke-dasharray`アニメーション） |
| 「国学博士」到達の金の脈動 | 「免状（めんじょう）」を授与される巻物風の証書デザインに転用 |

---

## フェーズ3：助動詞識別ゲームの実装設計

### 3-1. 画面構成の変更

```
screen-brand-title（タイトル画面）
  └ 「はじめる」
      └ screen-character-select（初回のみ）
          └ screen-mode-select（★新規：活用表 / 助動詞識別 の選択画面）
              ├ screen-title（既存：活用表ゲームの難易度選択）
              └ screen-shikibetsu-title（★新規：識別ゲームの難易度選択）
```

`SCREEN_IDS`配列に`screen-mode-select`・`screen-shikibetsu-title`・`screen-shikibetsu-game`を追加し、既存の`showScreen()`の仕組みをそのまま使い回せます。

### 3-2. 出題データ設計

既存の`JODOUSHI_EXPLANATIONS`（意味・接続・識別のポイント・例文を持つ解説データ）を**そのまま再利用**できる設計にします。問題データだけを新規に定義します。

```js
// 問題データ（識別ゲーム専用）。既存のJODOUSHI_EXPLANATIONSと同じidで紐づける。
const SHIKIBETSU_QUESTIONS = [
  {
    id: "q001",
    difficulty: "beginner",        // beginner / intermediate / advanced
    format: "name",                // name(①) / meaning(②) / katsuyokei(③) / context(④) / full(⑤)
    patternId: "keri",             // JODOUSHI_EXPLANATIONSのキーと対応
    prompt: "けり",
    choices: ["けり", "き", "らむ", "べし"],
    answer: "けり",
  },
  {
    id: "q002",
    difficulty: "advanced",
    format: "context",
    patternId: "nu-kanryo",
    sentenceBefore: "花",
    targetWord: "咲きぬ",         // ハイライト対象（中級は色付け表示、上級は非表示にする）
    sentenceAfter: "。",
    question: "「ぬ」の文法的意味は？",
    choices: ["完了", "打消の連体形", "推量", "断定"],
    answer: "完了",
  },
  // ...
];
```

出題形式ごとの生成ロジックは、既存の`getRandomPatterns()`と同じ「シャッフルして必要数を抽出する」関数を流用できます。

### 3-3. 難易度と出題形式の対応（ご提案の再掲・実装レベルでの整理）

| 難易度 | 採用フォーマット | 選択肢数 | 出題プール |
|---|---|---|---|
| 初級 | ①名前当て／②意味当て | 4択 | 意味が1つしかない助動詞を優先（き・けり・ず 等） |
| 中級 | ③活用形当て／④文中識別（対象語ハイライトあり） | 4〜6択 | 複数の意味を持つ助動詞も含める（む・べし 等） |
| 上級 | ④文中識別（ハイライトなし）／⑤総合問題（名前→活用形→意味の連続回答） | 4〜6択 | 識別の定番紛らわしい語を厚めに配分（ぬ／なむ／らむ・らる／に） |

### 3-4. ゲームサイクルの設計

- 1セット＝5問（初級）〜10問（上級）
- セット終了時に「クリア画面」を表示（既存の活用表ゲームのクリア画面パターンを流用：正答率・連続正解数・獲得EXPを表示）
- 「もう1セット」ボタンで即座に次のセットへ（短時間サイクルを重視）
- 誤答した問題は`weakPoints`に記録し、「苦手克服モード」（誤答の多い問題を優先出題する特別セット）を選択画面に追加

### 3-5. 共有データ構造（実装レベル）

既存の`playerData.modules`にエントリを追加するだけで完結します。

```js
// createDefaultModules() に追加する想定
jodoushi_shikibetsu: {
  displayName: "助動詞識別ゲーム",
  status: "available",
  playCount: 0,
  correctCount: 0,
  totalAnswered: 0,
  bestStreak: 0,
  weakPoints: {}, // { patternId: 誤答回数 }
},
```

EXP付与は、既存の`awardExp()`をそのまま呼び出します。識別ゲーム独自のEXP計算式案：

```js
// 1問正解ごとに基礎EXP、連続正解でボーナス、セット完走でボーナス
const baseExp = 8;
const streakBonus = Math.min(streak * 2, 20); // 連続正解ボーナス（上限あり）
const setCompleteBonus = allCorrect ? 30 : 0;  // 全問正解でのセット完走ボーナス
awardExp(baseExp + streakBonus + setCompleteBonus);
```

活用表ゲームと同じ`awardExp()`を経由するため、**レベル・称号・成長段階（＝立ち絵）はすべて自動的に共通化**されます。

### 3-6. 学習メモとの連携

正解・不正解にかかわらず、既存の`JODOUSHI_EXPLANATIONS`を使った解説カード（意味・接続・識別のポイント・例文）を毎問後に表示する設計を推奨します。特に**不正解時**は、既存の学習メモモーダルと同じUIパターンを再利用し「なぜ間違えたか」を即座に振り返れるようにします（テスト対策としての効果を最大化するための設計）。

---

## 実装順序のご提案

1. **フェーズ2（世界観）を先に実装** — 背景・装飾・演出のトーンを確定させます
2. **フェーズ3（識別ゲーム）を、フェーズ2で確定した世界観の上に実装** — 二度手間を避けます

フェーズ2から着手してよろしければ、次のメッセージで実装に入ります。配色（深緑・えんじ・墨・金の具体的な使い分け）や、月・桜の代替モチーフ（巻物／文箱、墨の筆致）について、上記の提案通りで進めてよいかだけご確認いただければと思います。
